diff --git a/ImmersiveEquipmentDisplays/IED/Controller/IEquipment.cpp b/ImmersiveEquipmentDisplays/IED/Controller/IEquipment.cpp
--- a/ImmersiveEquipmentDisplays/IED/Controller/IEquipment.cpp
+++ b/ImmersiveEquipmentDisplays/IED/Controller/IEquipment.cpp
@@ -4,5 +4,6 @@
 
 #include "ActorObjectHolder.h"
 #include "BipedSlotData.h"
+#include "IEDSyncTogetherBridge.h"
 #include "IRNG.h"
 
@@ -45,6 +46,30 @@ namespace IED
 		const ObjectEntrySlot&    a_slot) noexcept
 		-> SelectedItem
 	{
+		std::uint32_t overrideFormID = 0;
+		switch (IEDSyncTogetherBridge::QuerySlot(
+			a_params.actor->formID.get(),
+			stl::underlying(a_slot.slotid),
+			overrideFormID))
+		{
+		case IEDSyncTogetherBridge::SlotOverrideResult::kEmpty:
+			return {};
+		case IEDSyncTogetherBridge::SlotOverrideResult::kForm:
+		{
+			auto it = std::find_if(
+				a_candidates.begin(),
+				a_candidates.end(),
+				[overrideFormID](const auto& a_item) [[msvc::forceinline]] {
+					return a_item.item->form->formID.get() == overrideFormID;
+				});
+
+			return it != a_candidates.end() ? SelectedItem{ it } : SelectedItem{};
+		}
+		case IEDSyncTogetherBridge::SlotOverrideResult::kNotRemote:
+		default:
+			break;
+		}
+
 		if (a_candidates.empty())
 		{
 			return {};
diff --git a/ImmersiveEquipmentDisplays/IED/Controller/Controller.cpp b/ImmersiveEquipmentDisplays/IED/Controller/Controller.cpp
--- a/ImmersiveEquipmentDisplays/IED/Controller/Controller.cpp
+++ b/ImmersiveEquipmentDisplays/IED/Controller/Controller.cpp
@@ -2,6 +2,7 @@
 
 #include "Controller.h"
+#include "IEDSyncTogetherBridge.h"
 
 #include "IConditionalVariableProcessor.h"
 #include "IMaintenance.h"
@@ -3425,8 +3426,45 @@ namespace IED
 	{
 		auto& pluginMap = a_params.objects.GetCustom(a_class);
+		const bool remoteProxy = IEDSyncTogetherBridge::IsRemoteProxy(
+			a_params.actor->formID.get());
+		const stl::fixed_string iedSyncPlugin{ "IEDSyncTogether.esp" };
+
+		// A proxy may have been evaluated by ordinary NPC Displays before STRPM
+		// published its mapping. Purge any already-created Custom Item entries
+		// not owned by IEDSyncTogether before processing the authoritative state.
+		if (remoteProxy)
+		{
+			for (auto it = pluginMap.begin(); it != pluginMap.end();)
+			{
+				if (it->first == iedSyncPlugin)
+				{
+					++it;
+					continue;
+				}
+
+				auto eraseIt = it++;
+				for (auto& entry : eraseIt->second)
+				{
+					if (RemoveObject(
+							a_params.actor,
+							a_params.handle,
+							entry.second,
+							a_params.objects,
+							a_params.flags,
+							false))
+					{
+						a_params.state.flags.set(ProcessStateUpdateFlags::kMenuUpdate);
+					}
+				}
+				pluginMap.erase(eraseIt);
+			}
+		}
 
 		for (auto& e : a_confPluginMap)
 		{
+			if (remoteProxy && e.first != iedSyncPlugin)
+			{
+				continue;
+			}
+
 			if (e.second.empty())
 			{
 				continue;
diff --git a/ImmersiveEquipmentDisplays/IED/Controller/IEDSyncTogetherBridge.h b/ImmersiveEquipmentDisplays/IED/Controller/IEDSyncTogetherBridge.h
new file mode 100644
--- /dev/null
+++ b/ImmersiveEquipmentDisplays/IED/Controller/IEDSyncTogetherBridge.h
@@ -0,0 +1,78 @@
+#pragma once
+
+#include <Windows.h>
+
+#include <cstdint>
+#include <memory>
+
+namespace IED::IEDSyncTogetherBridge
+{
+	enum class SlotOverrideResult : std::uint32_t
+	{
+		kNotRemote = 0,
+		kEmpty = 1,
+		kForm = 2
+	};
+
+	using query_slot_override_t = std::uint32_t(__cdecl*)(
+		std::uint32_t actorFormID,
+		std::uint32_t slotIndex,
+		std::uint32_t* outFormID) noexcept;
+
+	using is_remote_proxy_t = std::uint32_t(__cdecl*)(
+		std::uint32_t actorFormID) noexcept;
+
+	inline HMODULE GetIEDSyncTogetherModule() noexcept
+	{
+		return GetModuleHandleW(L"IEDSyncTogether.dll");
+	}
+
+	inline query_slot_override_t GetQueryFunction() noexcept
+	{
+		static query_slot_override_t function = nullptr;
+		if (!function)
+		{
+			if (const auto module = GetIEDSyncTogetherModule())
+			{
+				function = reinterpret_cast<query_slot_override_t>(
+					GetProcAddress(module, "IEDST_QuerySlotOverride"));
+			}
+		}
+		return function;
+	}
+
+	inline is_remote_proxy_t GetRemoteProxyFunction() noexcept
+	{
+		static is_remote_proxy_t function = nullptr;
+		if (!function)
+		{
+			if (const auto module = GetIEDSyncTogetherModule())
+			{
+				function = reinterpret_cast<is_remote_proxy_t>(
+					GetProcAddress(module, "IEDST_IsRemoteProxy"));
+			}
+		}
+		return function;
+	}
+
+	inline SlotOverrideResult QuerySlot(
+		std::uint32_t actorFormID,
+		std::uint32_t slotIndex,
+		std::uint32_t& outFormID) noexcept
+	{
+		outFormID = 0;
+		const auto function = GetQueryFunction();
+		if (!function)
+		{
+			return SlotOverrideResult::kNotRemote;
+		}
+		return static_cast<SlotOverrideResult>(
+			function(actorFormID, slotIndex, std::addressof(outFormID)));
+	}
+
+	inline bool IsRemoteProxy(std::uint32_t actorFormID) noexcept
+	{
+		const auto function = GetRemoteProxyFunction();
+		return function && function(actorFormID) != 0;
+	}
+}
